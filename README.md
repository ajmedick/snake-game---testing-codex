# Snake (Classic)

A minimal, classic Snake game built with plain HTML/CSS/JS.

## Run

Option 1: open `index.html` directly in your browser.

Option 2: run a simple local server:

```bash
python3 -m http.server
```

Then visit `http://localhost:8000`.

## Controls

- Arrow keys or WASD to move
- Space to pause/resume
- Restart button to reset

## Notes

- The game runs on a fixed grid with deterministic logic.
- On small screens, on‑screen direction buttons appear.
